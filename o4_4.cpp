#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int a, q;
    cin >> a;
    vector<int> s(a / 2);
    for (int i = 0; i < a / 2; i++)
    {
        cin >> q;
        s[i] = q;
    }
    vector<int> d(a / 2);
    for (int i = 0; i < a / 2; i++)
    {
        cin >> q;
        s[i] = q;
    }
    for (int i = 0; i < a / 2; i++)
    {
    }
}