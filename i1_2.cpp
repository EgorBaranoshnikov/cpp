#include <iostream>
#include <cstdio>

using namespace std;

int main() {
    int a, b;
    freopen("divide.in", "r", stdin);
    freopen("divide.out", "w", stdout);
    cin >> a >> b;
    cout << a / b;
}